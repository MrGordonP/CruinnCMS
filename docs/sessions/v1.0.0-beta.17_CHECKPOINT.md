# v1.0.0-beta.17 Session Checkpoint

Date: 2026-06-04  
Branch: main  
Version track: v1.0.0-beta.17  
Final commit: recorded in git log for this checkpoint cycle

## Session Scope

This session started as a production bugfix pass, expanded into schema-model restoration and dashboard/editor architecture alignment, then finished with subject association workflow fixes for Blog and Events.

## Completed Work

### 1) Critical regressions fixed

Files:
- CruinnCMS/config/routes.php
- CruinnCMS/src/Admin/Controllers/AdminImportController.php
- CruinnCMS/modules/social/src/Controllers/SocialController.php

Fixes:
- Restored missing admin route alias for site builder new page flow:
  - `/admin/site-builder/pages/new` now resolves correctly.
- Resolved import controller fatal caused by visibility mismatch on `$db` inheritance.
- Stabilized social distribute flow where subject linkage had schema drift:
  - Added defensive support for environments where subject linkage columns varied.

### 2) Canonical subject model restored

Files:
- CruinnCMS/migrations/core/031_restore_subject_id_columns.sql

Fixes:
- Reintroduced canonical `subject_id` columns where they had been removed by prior migration logic.
- Added backfill path from `subject_content` bridge to restore subject ownership semantics.

Outcome:
- Platform returns to the intended model: canonical `subject_id` where required, with `subject_content` acting as association/index bridge rather than replacement.

### 3) Dashboard/editor card architecture corrected

Files:
- CruinnCMS/src/Modules/ModuleRegistry.php
- CruinnCMS/templates/dashboard.php
- CruinnCMS/templates/admin/editor.php
- public_html/js/editor.js

Fixes and decisions:
- Preserved grouped default dashboard structure (no flattened catalog dump).
- Moved grouped dashboard card sourcing to registry-backed catalog data.
- Kept dashboard card grouping semantics intact while using the same source family as editor placeable cards.
- Split bundled quick-link cards into single-purpose virtual entries for better picker granularity.
- Grouped editor module-widget picker into clearer card families:
  - Function Cards
  - Summary Cards
  - Quick Link Cards

Outcome:
- Dashboard remains curated and grouped.
- Editor and dashboard now share consistent catalog-driven definitions without sacrificing original UX structure.

### 4) Dashboard runtime hardening (optional schema safety)

Files:
- CruinnCMS/src/Services/DashboardService.php
- CruinnCMS/templates/admin/widgets/member-profile.php

Fixes:
- Added defensive handling for optional/missing instance tables/columns (including notifications and optional member fields).
- Prevented fallback-style rendering triggered by fatal exceptions.

Outcome:
- Dashboard no longer hard-fails when optional schema pieces are absent.

### 5) Subjects workspace: Add existing for Articles and Events

Files:
- CruinnCMS/src/Controllers/SubjectController.php
- CruinnCMS/templates/admin/subjects/workspace.php
- CruinnCMS/config/routes.php

New capability:
- In Subject workspace summary sections for Articles and Events, each now supports:
  - `+ New`
  - `+ Add existing`

Implementation details:
- Added subject attach endpoints:
  - `POST /admin/subjects/{id}/articles/attach`
  - `POST /admin/subjects/{id}/events/attach`
- Added controller handlers:
  - `adminAttachArticle()`
  - `adminAttachEvent()`
- Added available candidate queries in `adminView()` to populate attach pickers.
- Attachment uses `INSERT IGNORE` into `subject_content` to avoid duplicate-link errors.

### 6) Blog/Events module flow alignment for subject context

Files:
- CruinnCMS/modules/blog/src/Controllers/ArticleController.php
- CruinnCMS/modules/events/src/Controllers/EventController.php
- CruinnCMS/templates/admin/subjects/workspace.php

Fixes:
- Subject-launched “new” flows now preselect subject on form load using `subject_id` query param:
  - Blog `adminNew()` preloads `articleSubjectIds`
  - Events `adminNew()` preloads `eventSubjectIds`
- Corrected Subject workspace Blog routes to canonical blog post routes:
  - New: `/admin/blog/posts/new`
  - Edit: `/admin/blog/posts/{id}/edit`

Outcome:
- “From Subject” create flow and “Add existing” flow both work in the intended module paths.

## Verification

Validation performed:
- Static/error checks returned no errors on edited files.
- Route/controller/template wiring verified for Subjects, Blog, and Events attachment paths.

## Notes / Follow-up

- Pattern is now established for `subject_content` attach UI in Subject workspace for Articles and Events.
- Same approach can be applied to additional subject-owned entities if required (for example files/folders or other module content).
